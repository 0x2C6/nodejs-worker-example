# NodeJS worker example
This sample project illustrates handling long running task which can run async in the backgorund

For example sending email, generating reports.

For running project
```
$ chmod +x build.sh
$ ./build.sh
```

I assume you have docker and kubernetes installed and configured on your local